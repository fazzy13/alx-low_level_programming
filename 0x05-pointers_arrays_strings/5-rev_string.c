#include <string.h>

/**
* rev_string - function that reverses a string.
*
* @s: string to reverse
*/
void rev_string(char *s)
{
	char len = strlen(s);
	int i;

	for (i = len; i >= 0; i--)
	{
		return (*(s + 1));
	}
}
