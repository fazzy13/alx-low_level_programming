#include <stdio.h>

/**
* main - Entry point to find base 16
*
* Return: exit point after finding base 16
*
*/

int main(void)
{
	int i;

	for (i = 0; i < 16; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
