#include <stdio.h>

/**
*main - entry point of the programm
*
*Return: the exit point of the programm
*
*/

int main(void)
{
	int i = '0';

	for (i = '0'; i < '10'; i++)
	{
		putchar(i);
	}

	putchar('\n');

	return (0);
}
